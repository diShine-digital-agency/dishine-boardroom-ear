# diShine Boardroom Ear - Configuration
# --------------------------------------------------

# Transcription Settings
model_size: "small"       # options: tiny, base, small, medium, large-v3
device: "auto"            # cpu, cuda, or mps (for Mac)
compute_type: "float32"   # int8, float16, float32 (float32 is safest for CPU)

# Strategic Analysis Settings
anthropic_api_key: ""     # Add your Claude API key here for Strategic Action Plans
auto_anonymize: true      # Scrub PII before sending to Claude

# Output Settings
output_format: "markdown" # txt, markdown, or both
